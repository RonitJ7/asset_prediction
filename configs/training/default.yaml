# ── GNN training hyperparameters ──
lr: 1.0e-4
max_lr: 3.0e-3
weight_decay: 1.0e-4
betas: [0.9, 0.999]
warmup_pct: 0.2          # fraction of steps for OneCycleLR warmup

epochs: 70
batch_size: 16

turnover_weight: 0.01    # penalise prediction churn
grad_clip: 1.0           # max gradient norm
