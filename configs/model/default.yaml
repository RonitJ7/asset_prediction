# ── GNN architecture ──
hidden_dim: 96
dropout: 0.1
lag_steps: 2
activation: elu         

# ── MLP (sklearn MLPClassifier) ──
mlp:
  layers: [64, 32]
  activation: relu
  alpha: 1.0e-4
  learning_rate_init: 1.0e-3
  max_iter: 50
