# ── GNN architecture ──
hidden_dim: 96
dropout: 0.1
lag_steps: 2
activation: elu         

# ── MLP architecture ──
mlp:
  layers: [32,64,32]
  dropout: 0.1
  activation: elu
  use_layernorm: false
  epochs: 40           
